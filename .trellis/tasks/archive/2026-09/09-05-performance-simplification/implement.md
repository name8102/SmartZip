# Execution

Trellis runtime/spec files remain absent; repository CONTEXT.md, docs/agents and task artifacts supply available contracts. Preserve current explicit user scope over older design aspirations.

1. Capture current release CLI data-operation baseline.
2. Budget scheduling/final Usage; focused cancellation and accounting checks.
3. DB batch APIs, matching ranking index migration, CLI streaming/HashSet cleanup.
4. Borrow directory indices and simplify/cache ZIP metadata within task lifetime.
5. Compare identical workload; run complete safety suite; document evaluation-only findings separately.

## Completion

Implemented the approved budget, batch/index, borrowed-volume and encoding-preparation changes. See report.md for measured results, provenance, Linux validation and evaluation-only decisions. Cross-stage ZIP container caching and raw classification are deferred because their error/skip behavior is not equivalent. Whole-extraction speedup was not established; both paired sample groups are preserved.
