# 基于当前主线清理任务执行层

用户要求在不破坏功能的前提下简化任务装配和状态管理，并提交推送。最初工作目录停留在 `24fccc9`，比审查建议和远端主线落后 19 次提交；那份实现及其验证结果不适用于本任务。旧目录中的修改保留，本任务从 `origin/main@f3b74a005856a23671f06f4eccc6ada2a62c0631` 的独立 worktree 重新实现，不移植旧补丁。

验收关注 CLI/GUI 新建与恢复任务使用自身配置，暂停/等待/取消准入维持原行为，事件和历史按任务聚合且有界，分卷锁后去重、持久化提交对账、恢复身份与预算保持正确。具体决策和验证结果见 `implement.md`。
