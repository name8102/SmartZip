# ZIP 原生分卷发现修复

- 复现：真实加密 `backup2026.zip` 与三个 `.zNN` 文件同目录，输入 `.zip` 报 `expected 4 volumes, found 1`。通用数字 token 候选非空，阻止了原生 ZIP 扩展名分组。
- 原生同名 `.z01 ... .zip` 组先于通用数字序列识别，继续走结构验证；扩展名兼容大小写，basename 保持原有精确匹配。独立 ZIP 的结构判断仍优先，缺卷仍报错。
- 分卷文件访问沿用源目录符号链接方案，本次没有引入卷数据复制。
- 验证：224 个引擎单元测试通过；真实加密 ZIP 手动密码回归覆盖普通名称、数字名称、`.zip` / `.z01` / `.Z02` 入口和大小写扩展名，比较解压内容字节；密码提示测试通过。单元回归覆盖混合大小写卷、缺卷和独立 ZIP 不误合并。
