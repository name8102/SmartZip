# 多任务、多文件阶段调度设计

日期：2026-09-19。类型：设计交付，非运行时实现。

用户要求：覆盖多任务、多文件的内嵌识别、解压、编码检测和预览，提升总体速度与资源利用率，支持优先级、排队调整和等待输入不阻塞；前序已确认纳入恢复，暂不做压缩创建，仅维护一套历史。

完整设计与实施切片统一在 [docs/task-system.md](../../../docs/task-system.md)，不在此重复状态机或参数。

本次核对了现有 GUI 队列/worker、BFS 工作流、PreparedArchive、编码流程、预算监控、事务提交、SQLite 历史及密码服务。设计保留核心安全契约，明确需要替换的串行边界；未更改运行代码、未执行 GUI 验收，也未把设计吞吐目标作为性能结果。

验证：文档链接与 diff 检查；审查等待资源释放、优先级饥饿、分卷去重、输入变化、提交崩溃及历史禁写场景。未来实现仍须满足设计第 12 节的真实后端与故障注入验证。
