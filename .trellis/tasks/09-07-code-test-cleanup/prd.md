# 清理冗余实现与测试

用户要求清理过度防御、无效兜底和无用测试，结合 cargo llvm-cov / cargo crap 补充有意义的测试，保持功能和可观察行为。

范围：基于完整 workspace 基线筛选有源码证据的内部冗余；重点处理密码访问、扫描策略、完整性报告归约和 CLI 测试缺口。保留公开请求/结果格式与安全、回滚、取消、资源预算的既有语义。

验收：受影响行为测试、完整 workspace 覆盖率运行、真实 CLI 后端验收、workspace all-targets 检查与格式/路由检查。具体结果只维护于 implement.md。
